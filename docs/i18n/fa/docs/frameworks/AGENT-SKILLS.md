---
title: "کاتالوگ Agent Skills مربوط به RouteChi"
version: 3.8.40
lastUpdated: 2026-06-28
---

# کاتالوگ Agent Skills مربوط به RouteChi

> **منبع اصلی:** `src/lib/agentSkills/` (catalog، generator، parserها) + دایرکتوری `skills/` (فایل‌های SKILL.md)
> **آخرین به‌روزرسانی:** 2026-06-28 — v3.8.40

Agent Skills فایل‌های ساختاریافته‌ی SKILL.md هستند که به عامل‌های خارجی، کلاینت‌های MCP و
orchestratorهای A2A یاد می‌دهند چگونه از REST API و CLI مربوط به RouteChi استفاده کنند. برخلاف
[Omni Skills](./SKILLS.md) (که تعاریف ابزار LLM هستند و داخل RouteChi اجرا می‌شوند)،
Agent Skills یک _کاتالوگ مستندات_ است — markdown استاتیک که می‌تواند مستقیماً به context عامل تزریق شود.

---

## نمای کلی

کاتالوگ شامل **۴۲ Agent Skill کانونیک** (۲۲ REST API + ۲۰ CLI) است. هر مهارت دارای:

- یک **ID کانونیک** (`omni-auth`، `cli-serve` و غیره)
- یک فایل **SKILL.md** در `skills/{id}/SKILL.md` با YAML frontmatter (`name`، `description`) + بدنه‌ی markdown غنی
- **endpointهای REST** (مهارت‌های API) یا **subcommandهای CLI** (مهارت‌های CLI) که از spec مربوط به OpenAPI و رجیستری CLI استخراج شده‌اند
- یک **URL خام GitHub** برای fetch زنده: `https://raw.githubusercontent.com/borhandarabi/routechi/refs/heads/main/skills/{id}/SKILL.md`

---

## معماری

```
src/shared/constants/agentSkills.ts    — 42-entry curated list (name/desc/category/area/icon)
src/lib/agentSkills/
  catalog.ts                           — getCatalog(), getSkillById(), filterCatalog(), computeCoverage()
  generator.ts                         — generateAgentSkills() writes SKILL.md to skills/{id}/
  openapiParser.ts                     — extracts REST endpoints from docs/openapi.yaml
  cliRegistryParser.ts                 — extracts CLI subcommands from bin/cli-registry.ts
  schemas.ts                           — Zod schemas: AgentSkillSchema, SkillCoverageSchema, etc.
  types.ts                             — TypeScript interfaces: AgentSkill, SkillCoverage, etc.

skills/{id}/SKILL.md                   — Generated + curated markdown files (42 total)

src/app/api/agent-skills/
  route.ts                             — GET /api/agent-skills
  [id]/route.ts                        — GET /api/agent-skills/{id}
  [id]/raw/route.ts                    — GET /api/agent-skills/{id}/raw (text/markdown)
  coverage/route.ts                    — GET /api/agent-skills/coverage
  generate/route.ts                    — POST /api/agent-skills/generate (auth required)

open-sse/mcp-server/tools/agentSkillTools.ts  — 3 MCP tools (list, get, coverage)
src/lib/a2a/skills/listCapabilities.ts        — A2A skill: list-capabilities
```

---

## فرمت SKILL.md

```markdown
---
name: omni-providers
description: "Manage provider connections: add, test, rotate, and remove credentials."
---

<!-- generated by src/lib/agentSkills/generator.ts; manual edits will be overwritten -->

## Overview

...

## Authentication

...

## Endpoints

...

<!-- skill:custom-start -->

## Custom Section (preserved across regeneration)

...

<!-- skill:custom-end -->
```

مولد محتوا بین `<!-- skill:custom-start -->` و `<!-- skill:custom-end -->` را در زمان تولید مجدد حفظ می‌کند. ده مهارت دارای بلوک‌های سفارشی curated هستند:

`omni-mcp`، `omni-compression`، `cli-providers`، `cli-eval`، `omni-agents-a2a`، `omni-combos-routing`، `omni-auth`، `omni-resilience`، `omni-inference`، `cli-serve`.

---

## کشف REST API

| Endpoint                     | Method | توضیحات                                              | احراز هویت |
| :--------------------------- | :----- | :--------------------------------------------------- | :--------- |
| `/api/agent-skills`          | GET    | فهرست کاتالوگ (اختیاری `?category=api\|cli&area=<area>`) | هیچ‌کدام    |
| `/api/agent-skills/{id}`     | GET    | دریافت متادیتای یک مهارت                             | هیچ‌کدام    |
| `/api/agent-skills/{id}/raw` | GET    | fetch SKILL.md به‌صورت `text/markdown`                | هیچ‌کدام    |
| `/api/agent-skills/coverage` | GET    | آمار پوشش (چه تعداد فایل SKILL.md وجود دارد)         | هیچ‌کدام    |
| `/api/agent-skills/generate` | POST   | راه‌اندازی مولد (dryRun/prune/onlyIds)               | مدیریت     |

مثال — فهرست همه‌ی مهارت‌های API:

```bash
curl "http://localhost:20128/api/agent-skills?category=api"
```

مثال — fetch یک SKILL.md:

```bash
curl -H "Accept: text/markdown" "http://localhost:20128/api/agent-skills/omni-providers/raw"
```

---

## کشف MCP

سه ابزار MCP تحت scope `read:catalog` ثبت شده‌اند:

| ابزار                              | توضیحات                                            |
| :--------------------------------- | :------------------------------------------------- |
| `omniroute_agent_skills_list`     | فهرست مهارت‌ها (فیلترهای اختیاری `category` / `area`) |
| `omniroute_agent_skills_get`      | دریافت متادیتا + SKILL.md برای یک مهارت بر اساس `id` |
| `omniroute_agent_skills_coverage` | آمار پوشش (API/CLI have/total)                     |

برای اتصال scope و احراز هویت به [MCP-SERVER.md](./MCP-SERVER.md) مراجعه کنید.

---

## کشف A2A

مهارت A2A `list-capabilities` کل کاتالوگ ۴۲ مهارتی را به‌عنوان یک artifact جدول markdown برمی‌گرداند. orchestratorهای خارجی می‌توانند آن را از طریق این فراخوانی کنند:

```json
{
  "jsonrpc": "2.0",
  "id": "1",
  "method": "message/send",
  "params": {
    "skill": "list-capabilities",
    "messages": [{ "role": "user", "content": "List all capabilities" }]
  }
}
```

برای جزئیات پروتکل به [A2A-SERVER.md](./A2A-SERVER.md) مراجعه کنید.

---

## کاتالوگ — ۴۲ Skill ID

### مهارت‌های API (22)

| ID                     | حوزه           | نقطه‌ی ورود                         |
| :--------------------- | :------------- | :---------------------------------- |
| `omni-auth`            | auth           | احراز هویت + مدیریت نشست            |
| `omni-providers`       | providers      | مدیریت اتصال provider               |
| `omni-models`          | models         | کاتالوگ مدل و قابلیت‌ها             |
| `omni-combos-routing`  | combos-routing | استراتژی‌های routing combo          |
| `omni-api-keys`        | api-keys       | مدیریت API key                      |
| `omni-usage-logs`      | usage-logs     | logهای استفاده و هزینه              |
| `omni-budget`          | budget         | budget guardها                      |
| `omni-settings`        | settings       | تنظیمات سراسری                      |
| `omni-proxies`         | proxies        | مدیریت proxy pool                   |
| `omni-cache`           | cache          | cache معنایی + prompt               |
| `omni-compression`     | compression    | موتورهای فشرده‌سازی context         |
| `omni-context-rtk`     | context-rtk    | فشرده‌سازی RTK                      |
| `omni-resilience`      | resilience     | circuit breakerها + cooldownها      |
| `omni-cli-tools`       | cli-tools      | REST proxy ابزارهای CLI             |
| `omni-tunnels`         | tunnels        | مدیریت tunnel                       |
| `omni-sync-cloud`      | sync-cloud     | همگام‌سازی ابری                      |
| `omni-db-backups`      | db-backups     | backupهای پایگاه داده               |
| `omni-webhooks`        | webhooks       | مبدل ارسال رویداد وب‌هوک            |
| `omni-mcp`             | mcp            | سرور MCP (۸۷ ابزار، ۳ ترنسپورت)     |
| `omni-agents-a2a`      | agents-a2a     | پروتکل عامل A2A                     |
| `omni-version-manager` | version-manager| مدیریت نسخه و به‌روزرسانی          |
| `omni-inference`       | inference      | inference مستقیم / completionها     |

### مهارت‌های CLI (20)

| ID                   | حوزه              | ریشه‌ی فرمان CLI        |
| :------------------- | :---------------- | :---------------------- |
| `cli-serve`          | cli-serve         | `routechi serve`       |
| `cli-health`         | cli-health        | `routechi health`      |
| `cli-providers`      | cli-providers     | `routechi providers`   |
| `cli-keys`           | cli-keys          | `routechi keys`        |
| `cli-models`         | cli-models        | `routechi models`      |
| `cli-chat`           | cli-chat          | `routechi chat`        |
| `cli-routing`        | cli-routing       | `routechi routing`     |
| `cli-resilience`     | cli-resilience    | `routechi resilience`  |
| `cli-compression`    | cli-compression   | `routechi compression` |
| `cli-contexts`       | cli-contexts      | `routechi contexts`    |
| `cli-cost-usage`     | cli-cost-usage    | `routechi cost`        |
| `cli-mcp`            | cli-mcp           | `routechi mcp`         |
| `cli-a2a`            | cli-a2a           | `routechi a2a`         |
| `cli-tunnel`         | cli-tunnel        | `routechi tunnel`      |
| `cli-backup-sync`    | cli-backup-sync   | `routechi backup`      |
| `cli-policy-audit`   | cli-policy-audit  | `routechi policy`      |
| `cli-batches`        | cli-batches       | `routechi batch`       |
| `cli-eval`           | cli-eval          | `routechi eval`        |
| `cli-plugins-skills` | cli-plugins-skills| `routechi plugins`     |
| `cli-setup`          | cli-setup         | `routechi setup`       |

---

## نحوه‌ی مصرف مهارت‌ها توسط عامل‌های خارجی

### 1. کشف از طریق REST

```bash
# Get the full catalog
curl "http://your-omniroute/api/agent-skills" | jq '.skills[] | {id, name, category}'

# Get SKILL.md for context injection
curl "http://your-omniroute/api/agent-skills/omni-providers/raw" > omni-providers.md
```

### 2. کشف از طریق MCP

```typescript
// In a Claude Desktop / Cursor MCP client:
const result = await client.callTool("omniroute_agent_skills_list", { category: "api" });
// result.skills → array of AgentSkill with rawUrl for each
```

### 3. کشف از طریق A2A

```python
import requests

resp = requests.post("http://your-omniroute/a2a", json={
    "jsonrpc": "2.0", "id": "1",
    "method": "message/send",
    "params": {"skill": "list-capabilities", "messages": [{"role": "user", "content": "list"}]}
})
table = resp.json()["result"]["artifacts"][0]["content"]
# table is a markdown table with all 42 skill IDs + rawUrl columns
```

### 4. fetch خام مستقیم GitHub (بدون نیاز به سرور)

```bash
BASE="https://raw.githubusercontent.com/borhandarabi/routechi/refs/heads/main/skills"
curl "${BASE}/omni-providers/SKILL.md"
```

---

## مولد

مولد کاتالوگ curated + spec مربوط به OpenAPI + رجیستری CLI را می‌خواند و `skills/{id}/SKILL.md` را برای هر ورودی می‌نویسد:

```bash
# Preview (dry run, no writes)
curl -X POST http://localhost:20128/api/agent-skills/generate \
  -H "Authorization: Bearer <admin-key>" \
  -H "Content-Type: application/json" \
  -d '{"dryRun":true}'

# Full regeneration
curl -X POST http://localhost:20128/api/agent-skills/generate \
  -H "Authorization: Bearer <admin-key>" \
  -H "Content-Type: application/json" \
  -d '{"dryRun":false,"prune":false}'

# Regenerate specific IDs
curl -X POST http://localhost:20128/api/agent-skills/generate \
  -H "Authorization: Bearer <admin-key>" \
  -H "Content-Type: application/json" \
  -d '{"dryRun":false,"onlyIds":["omni-providers","cli-serve"]}'
```

پاسخ مولد یک `GeneratorReport` است:

```json
{
  "generated": ["omni-providers", "cli-serve"],
  "unchanged": [],
  "pruned": [],
  "orphansDetected": [],
  "errors": []
}
```

---

## API پوشش

```bash
curl "http://localhost:20128/api/agent-skills/coverage"
```

```json
{
  "api": { "have": 22, "total": 22 },
  "cli": { "have": 20, "total": 20 },
  "totalSkills": 42,
  "generatedAt": "2026-05-28T00:00:00.000Z"
}
```

---

## مرتبط

- [SKILLS.md](./SKILLS.md) — فریم‌ورک Omni Skills (تزریق ابزار LLM + marketplace)
- [MCP-SERVER.md](./MCP-SERVER.md) — کاتالوگ ابزار MCP (ابزارهای `omniroute_agent_skills_*`)
- [A2A-SERVER.md](./A2A-SERVER.md) — پروتکل A2A (مهارت `list-capabilities`)
- `src/lib/agentSkills/` — کاتالوگ، مولد، parserها
- `skills/` — فایل‌های SKILL.md تولیدشده (۴۲ ورودی)
