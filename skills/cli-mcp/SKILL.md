---
name: cli-mcp
description: Inspect the MCP server status, list registered tools and scopes, run tool invocations, and manage MCP audit logs from the CLI.
---
<!-- generated by src/lib/agentSkills/generator.ts; manual edits will be overwritten -->

## Overview

Inspect the MCP server status, list registered tools and scopes, run tool invocations, and manage MCP audit logs from the CLI.

## Quick install

```bash
npm install -g routechi   # or: npx routechi
routechi --version
```

## Subcommands

### `mcp`

**Example:**

```bash
routechi mcp
```

### `mcp status`

Show MCP server status

**Flags:**

- `--json`

**Example:**

```bash
routechi mcp status
```

### `mcp restart`

Restart the MCP server

**Example:**

```bash
routechi mcp restart
```

### `mcp call <tool> [argsJson]`

**Flags:**

- `--args <json>`
- `--args-file <path>`
- `--stream`
- `--scope <s>`

**Example:**

```bash
routechi mcp call <tool> [argsJson]
```

### `mcp scopes`

**Flags:**

- `--tool <name>`

**Example:**

```bash
routechi mcp scopes
```

### `mcp tools`

**Example:**

```bash
routechi mcp tools
```

### `mcp list`

**Flags:**

- `--scope <s>`

**Example:**

```bash
routechi mcp list
```

### `mcp info <name>`

**Example:**

```bash
routechi mcp info <name>
```

### `mcp schema <name>`

**Flags:**

- `--io <kind>`

**Example:**

```bash
routechi mcp schema <name>
```

### `mcp audit`

**Example:**

```bash
routechi mcp audit
```

### `mcp tail`

**Flags:**

- `--follow`
- `--limit <n>`

**Example:**

```bash
routechi mcp tail
```

### `mcp stats`

**Flags:**

- `--period <p>`

**Example:**

```bash
routechi mcp stats
```
