---
name: cli-cost-usage
description: View cost breakdowns, token usage, and call logs from the CLI. Filter by provider, model, or date range. Export usage reports and inspect per-connection spending.
---
<!-- generated by src/lib/agentSkills/generator.ts; manual edits will be overwritten -->

## Overview

View cost breakdowns, token usage, and call logs from the CLI. Filter by provider, model, or date range. Export usage reports and inspect per-connection spending.

## Quick install

```bash
npm install -g routechi   # or: npx routechi
routechi --version
```

## Subcommands

### `cost`

**Flags:**

- `--period <range>`
- `--since <date>`
- `--until <date>`
- `--group-by <field>`
- `--api-key <key>`
- `--limit <n>`

**Example:**

```bash
routechi cost
```

### `pricing`

**Example:**

```bash
routechi pricing
```

### `pricing sync`

**Flags:**

- `--provider <p>`
- `--force`

**Example:**

```bash
routechi pricing sync
```

### `pricing list`

**Flags:**

- `--provider <p>`
- `--model <m>`
- `--limit <n>`

**Example:**

```bash
routechi pricing list
```

### `pricing get <model>`

**Example:**

```bash
routechi pricing get <model>
```

### `pricing defaults`

**Example:**

```bash
routechi pricing defaults
```

### `pricing show`

**Example:**

```bash
routechi pricing show
```

### `pricing set`

**Flags:**

- `--input <p>`
- `--output <p>`
- `--cache-read <p>`
- `--cache-write <p>`

**Example:**

```bash
routechi pricing set
```

### `pricing diff`

**Flags:**

- `--model <m>`

**Example:**

```bash
routechi pricing diff
```

### `usage`

**Example:**

```bash
routechi usage
```

### `usage analytics`

**Flags:**

- `--period <range>`
- `--provider <id>`

**Example:**

```bash
routechi usage analytics
```

### `usage budget`

**Example:**

```bash
routechi usage budget
```

### `usage list`

**Example:**

```bash
routechi usage list
```

### `usage get [scope]`

**Example:**

```bash
routechi usage get [scope]
```

### `usage set <amount>`

**Flags:**

- `--scope <s>`
- `--period <p>`

**Example:**

```bash
routechi usage set <amount>
```

### `usage reset [scope]`

**Example:**

```bash
routechi usage reset [scope]
```

### `usage quota`

**Flags:**

- `--provider <id>`
- `--check`

**Example:**

```bash
routechi usage quota
```

### `usage logs`

**Flags:**

- `--limit <n>`
- `--search <q>`
- `--since <ts>`
- `--follow`
- `--api-key <k>`

**Example:**

```bash
routechi usage logs
```

### `usage utilization`

**Flags:**

- `--api-key <k>`

**Example:**

```bash
routechi usage utilization
```

### `usage history`

**Flags:**

- `--limit <n>`

**Example:**

```bash
routechi usage history
```

### `usage proxy-logs`

**Flags:**

- `--limit <n>`

**Example:**

```bash
routechi usage proxy-logs
```
