---
name: cli-contexts
description: Manage context engineering configurations, RTK filter sets, and conversation sessions from the CLI. Apply context-relay settings and inspect active context pipelines.
---
<!-- generated by src/lib/agentSkills/generator.ts; manual edits will be overwritten -->

## Overview

Manage context engineering configurations, RTK filter sets, and conversation sessions from the CLI. Apply context-relay settings and inspect active context pipelines.

## Quick install

```bash
npm install -g routechi   # or: npx routechi
routechi --version
```

## Subcommands

### `context-eng`

**Example:**

```bash
routechi context-eng
```

### `context-eng analytics`

**Flags:**

- `--period <p>`

**Example:**

```bash
routechi context-eng analytics
```

### `context-eng caveman`

**Example:**

```bash
routechi context-eng caveman
```

### `context-eng config`

**Example:**

```bash
routechi context-eng config
```

### `context-eng show`

**Example:**

```bash
routechi context-eng show
```

### `context-eng set`

**Flags:**

- `--aggressiveness <n>`
- `--max-shrink-pct <n>`
- `--preserve-tags <list>`

**Example:**

```bash
routechi context-eng set
```

### `context-eng rtk`

**Example:**

```bash
routechi context-eng rtk
```

### `context-eng config`

**Example:**

```bash
routechi context-eng config
```

### `context-eng show`

**Example:**

```bash
routechi context-eng show
```

### `context-eng set`

**Flags:**

- `--token-budget <n>`
- `--reserve-pct <n>`

**Example:**

```bash
routechi context-eng set
```

### `context-eng filters`

**Example:**

```bash
routechi context-eng filters
```

### `context-eng list`

**Example:**

```bash
routechi context-eng list
```

### `context-eng add`

**Flags:**

- `--priority <n>`
- `--action <a>`

**Example:**

```bash
routechi context-eng add
```

### `context-eng remove <id>`

**Flags:**

- `--yes`

**Example:**

```bash
routechi context-eng remove <id>
```

### `context-eng test`

**Example:**

```bash
routechi context-eng test
```

### `context-eng raw-output <id>`

**Example:**

```bash
routechi context-eng raw-output <id>
```

### `context-eng combos`

**Example:**

```bash
routechi context-eng combos
```

### `context-eng list`

**Example:**

```bash
routechi context-eng list
```

### `context-eng get <id>`

**Example:**

```bash
routechi context-eng get <id>
```

### `context-eng assignments <id>`

**Example:**

```bash
routechi context-eng assignments <id>
```

### `contexts`

**Example:**

```bash
routechi contexts
```

### `contexts list`

List all contexts

**Example:**

```bash
routechi contexts list
```

### `contexts add <name>`

Add a new context

**Flags:**

- `--api-key <k>`
- `--api-key-stdin`
- `--access-token <t>`
- `--access-token-stdin`
- `--scope <s>`
- `--description <d>`

**Example:**

```bash
routechi contexts add <name>
```

### `contexts use <name>`

Switch active context

**Example:**

```bash
routechi contexts use <name>
```

### `contexts current`

Show the active context (server, auth, scope)

**Flags:**

- `--name-only`

**Example:**

```bash
routechi contexts current
```

### `contexts show <name>`

Show context details

**Example:**

```bash
routechi contexts show <name>
```

### `contexts remove <name>`

Remove a context

**Flags:**

- `--yes`

**Example:**

```bash
routechi contexts remove <name>
```

### `contexts rename <old> <new>`

Rename a context

**Example:**

```bash
routechi contexts rename <old> <new>
```

### `contexts export`

Export contexts to JSON

**Flags:**

- `--out <path>`
- `--no-secrets`

**Example:**

```bash
routechi contexts export
```

### `contexts import <file>`

Import contexts from a JSON file

**Flags:**

- `--merge`

**Example:**

```bash
routechi contexts import <file>
```

### `sessions`

**Example:**

```bash
routechi sessions
```

### `sessions list`

**Flags:**

- `--user <u>`
- `--kind <k>`
- `--active`
- `--limit <n>`

**Example:**

```bash
routechi sessions list
```

### `sessions show <sessionId>`

**Example:**

```bash
routechi sessions show <sessionId>
```

### `sessions expire <sessionId>`

**Flags:**

- `--yes`

**Example:**

```bash
routechi sessions expire <sessionId>
```

### `sessions expire-all`

**Flags:**

- `--yes`

**Example:**

```bash
routechi sessions expire-all
```

### `sessions current`

**Example:**

```bash
routechi sessions current
```
